# 2026-07-17

## 第1步完成：后端基础骨架
- 创建 `backend/` 目录，搭建 Flask 3.0.3 + SQLAlchemy 2.0 + SQLite 后端基础
- 文件清单：
  - `backend/requirements.txt` — Flask / flask-cors / flask-sqlalchemy 依赖
  - `backend/db.py` — SQLAlchemy 实例 + init_db() 初始化函数，自动创建 instance/ 目录
  - `backend/models.py` — Task 数据模型（7个字段：id, title, description, status, priority, created_at, updated_at），含 to_dict() 序列化方法
  - `backend/app.py` — Flask 主入口，CORS 配置 + 健康检查接口 /api/health
- 验证通过：数据库初始化成功，tasks 表已创建
