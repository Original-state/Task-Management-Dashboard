# 2026-07-19

## 详情页凭证图片展示
- 详情页表单上方新增独立"已上传凭证"展示区，大图显示（max-h-64），点击可在新标签页看原图
- TaskForm 内图片预览改为仅在新选取文件时显示（imageProofChanged 标记），避免与上方展示重复
- 0 ERROR

## 保存成功提示 + 自动返回看板
- 新建 `components/Toast.tsx` 通知组件，绿色浮动提示，1.5s 后淡出
- 创建页：保存成功后显示"创建成功"Toast → 自动跳转回任务看板
- 编辑页：保存成功后显示"保存成功"Toast → 自动跳转回任务看板
- 保存失败仍用 alert 提示错误
- 所有文件 0 ERROR

## 导入导出（改为单个任务）
- **用户反馈**：导出/导入应针对单个任务，不是全部任务
- 后端：`GET /api/tasks/<id>/export` 替代原来的全量导出接口
- 前端 api.ts：`exportTask(id)` 替代 `exportTasks()`
- TaskCard 每个卡片标题旁新增 📥 导出按钮，点击询问是否含图片 → 下载 `task_{id}.json`
- page.tsx 顶部仅保留 📤 导入按钮，移除了全量导出按钮
- 导入同时支持 `{ task: {...} }` 和 `{ tasks: [...] }` 两种格式，导出的单任务文件可直接重新导入
- 所有文件 0 ERROR

## 部署方案改造：Render → 双 Vercel 项目
- Render 免费 Web Service 需绑卡，无法通过 → 改用 Vercel（免绑卡）
- backend/db.py：新增 `os.getenv("VERCEL")` 判断，Vercel 环境用 `/tmp/taskmaster.db`（重启即清空）
- backend/requirements.txt：移除 gunicorn（Vercel 不需要）
- 删除 backend/Procfile、backend/runtime.txt（Render 专用）
- 新增 backend/vercel.json（maxDuration 30s）、backend/api/index.py（Vercel Flask 入口）
- frontend/next.config.js：rewrites 仅 `!API_BASE` 时生效（本地开发），生产由前端直接请求后端绝对 URL
- Git commit `89584e7` 已推送到 GitHub



